// src/theme/useTheme.ts
// Re-export so consumers can import from a stable path.
// The actual hook lives in ThemeProvider.tsx (same file as the context).

export { useTheme } from "./ThemeProvider";
