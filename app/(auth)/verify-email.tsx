// app/(auth)/verify-email.tsx
// Stub. TODO(arch §10): email verification handoff from deep link.

import { Screen } from "@/ui/Screen";
import { Text } from "@/ui/Text";

export default function VerifyEmail() {
  return (
    <Screen>
      <Text variant="heading">verify-email</Text>
      <Text color="secondary">Stub screen — arch §10</Text>
    </Screen>
  );
}
