// app/(tabs)/leaderboard/index.tsx
// Stub. TODO(arch §8.2 qk.leaderboard): wire to leaderboard endpoint.

import { Screen } from "@/ui/Screen";
import { Text } from "@/ui/Text";

export default function Leaderboard() {
  return (
    <Screen>
      <Text variant="heading">leaderboard</Text>
      <Text color="secondary">Stub screen — arch §8.2</Text>
    </Screen>
  );
}
