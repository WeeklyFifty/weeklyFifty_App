// app/(tabs)/settings/delete-account.tsx
// Stub. TODO(arch §10.6, §17.3): in-app deletion flow per Apple
// Guideline 5.1.1(v).

import { Screen } from "@/ui/Screen";
import { Text } from "@/ui/Text";

export default function DeleteAccount() {
  return (
    <Screen>
      <Text variant="heading">delete-account</Text>
      <Text color="secondary">Stub screen — arch §10.6, §17.3</Text>
    </Screen>
  );
}
