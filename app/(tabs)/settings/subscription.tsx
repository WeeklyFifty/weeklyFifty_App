// app/(tabs)/settings/subscription.tsx
// Stub. TODO(arch §11): show current plan, manage, restore.

import { Screen } from "@/ui/Screen";
import { Text } from "@/ui/Text";

export default function Subscription() {
  return (
    <Screen>
      <Text variant="heading">subscription</Text>
      <Text color="secondary">Stub screen — arch §11</Text>
    </Screen>
  );
}
