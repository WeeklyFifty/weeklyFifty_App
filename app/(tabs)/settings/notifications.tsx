// app/(tabs)/settings/notifications.tsx
// Stub. TODO(arch §12.5, §18.3): channel-toggle UI driven by the
// notification channel registry.

import { Screen } from "@/ui/Screen";
import { Text } from "@/ui/Text";

export default function Notifications() {
  return (
    <Screen>
      <Text variant="heading">notifications</Text>
      <Text color="secondary">Stub screen — arch §12.5, §18.3</Text>
    </Screen>
  );
}
